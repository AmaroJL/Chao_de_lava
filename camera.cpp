#include "camera.h"
#include <GL/glut.h>

// ANDRÈ

void atualizarCamera(float alvoX, float alvoY, float alvoZ) {
    // Configurar o gluLookAt.
    // A câmera deve ficar um pouco atrás e acima do alvo (personagem).

    // Exemplo de offset inicial: câmera em (alvoX, alvoY + 5, alvoZ + 10)
    // olhando para (alvoX, alvoY, alvoZ) com o UP vector (0, 1, 0).
    
}

